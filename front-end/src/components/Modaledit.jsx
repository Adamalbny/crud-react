import React from "react";

function Modaledit({angka}){
    // for(let i=0; i <= data.length(); i++){
    // data.forEach(element => {
    //     console.log(element[i]);
    // });}
    console.log(angka);
    function editJudul(){   
    };
    return(
        <div>
            
        </div>
    );
}

export default Modaledit;